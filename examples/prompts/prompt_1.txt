You will receive exactly one search query record.

Return only valid JSON and do not add markdown fences, explanations, or extra text.
The JSON must match this schema exactly:

{response_schema}

Echo the same `query` value from the user message.
The source row id is tracked separately as `{row_id_column}` / `custom_id`, so do not add it to the JSON output.
Return a single JSON object that matches the schema.
